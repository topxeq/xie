// 一个简单的计算器，输入一个表达式，并计算出结果
// A simple calculator that inputs an expression and calculates the result
getInput $expression "Please enter the expression: "

flexEval $result $expression

pln "result:" $result